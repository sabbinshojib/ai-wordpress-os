{
  "name": "ai-wordpress-os-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "React admin console for AI WordPress OS (built with esbuild)",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
