# AI WordPress OS — Bug / Gap Register

**Baseline commit:** `84d3c97bcd33d4d237a6e21593b6cecddbe36403`
**Companion document:** `docs/audits/ENTERPRISE-READINESS-AUDIT.md` (full narrative evidence)
**Scope:** Root-cause-grouped register. Cascading test failures are collapsed into their single root cause (noted in the "Evidence" column); they are not counted as separate bugs.

Status legend: `OPEN` (not fixed), `PLANNED` (scheduled in roadmap, not started), `RESOLVED` (fixed, tested, committed). Original findings are never rewritten or removed once resolved — only the Status/Resolution columns are added, so the historical record of what was found and why stays intact.

---

## Sprint 0.1 resolution summary (2026-09-06, branch `sprint/0.1-stabilization`)

All five confirmed BUG-00x items below are **RESOLVED**. Each fix shipped with regression tests and a coherent commit on `sprint/0.1-stabilization` (based on baseline `84d3c97`, not yet merged to `main`):

| Bug | Commit | Tests added |
|---|---|---|
| BUG-001 (PathGuard Windows) | `a16f359` fix(pathguard) | 9 new `PathGuardTest` cases |
| BUG-002 (REST sanitize_key) | `9bd693a` fix(rest) | `tests/Unit/ToolIdentifierTest.php` (16 cases) + 2 integration regressions |
| BUG-003 (PHPUnit bootstrap) | `61d614c` test(phpunit) | `tests/PHPUnit/NativeSuiteBridgeTest.php` (bridges every native class into real PHPUnit) |
| BUG-004 (extension guards) | `598ffea` fix(runtime) | `tests/Unit/StringsTest.php`, `tests/Unit/EnvironmentGuardTest.php` |
| BUG-005 (multisite lifecycle) | `b85feb0` fix(multisite) | `tests/Integration/MultisiteLifecycleTest.php` (11 cases) |

A sixth item — capability usability (`ai_os_use`/`ai_os_approve`, tracked under ARCH-005 below) — was also resolved this sprint in `c1fbf34` fix(capabilities), with `tests/Integration/CapabilityLifecycleTest.php` (9 cases). Full narrative in `docs/audits/SPRINT-0.1-STABILIZATION-REPORT.md`.

## Sprint 0.3A resolution summary (2026-09-07, branch `sprint/0.3-security-ci`)

All five SEC-M1..M5 hardening items and the remaining half of ARCH-005 (admin-grantable capability management) are now **RESOLVED**. Each fix shipped with regression tests and a coherent commit on `sprint/0.3-security-ci` (not yet merged to `main`):

| Item | Commit | Tests added |
|---|---|---|
| SEC-M1 (rate limiter race) | `b093e84` fix(security) | Atomic DB-backed counter (`RateLimitRepository`), `RateLimiterTest` |
| SEC-M2 (HTTPS trusts `Host` header) | `5eee086` fix(security) | `isLocalDevelopment()` no longer inspects any client-supplied header |
| SEC-M3 (`/tools/execute` missing nonce) | `765f2e1` fix(security) | `ToolsControllerNonceTest` (6 cases) |
| SEC-M4 (audit log has no tamper-evidence) | `f3d7aa3` feat(audit) | `AuditIntegrityTest` (22 cases) + `AuditChainTest` |
| SEC-M5 (`Crypto` has no key-rotation strategy) | `3f4e246` fix(crypto) | `CryptoTest` (31 cases: envelope parsing, malformed/unsupported-version/wrong-key/tamper/auth-failure, legacy detection, previous-key decrypt, re-encrypt, no-leak assertions, sodium/openssl paths) |
| ARCH-005 remainder (admin-grantable `ai_os_approve`/`ai_os_use`) | `ea452db` feat(security) | `CapabilityManagementTest` (21 cases: grant/revoke, no self-escalation, unauthorized-caller rejection, approval-gate consequence, audit, multisite isolation, REST surface) |
| REL-001/REL-002 (no CI, no static analysis config) | `8efb2de` build(ci) | Composer scripts (`stan`, `cs`, `test:phpunit`, `ci`), `phpstan.neon.dist`, `phpcs.xml.dist`, `.github/workflows/ci.yml` (PHP 8.2/8.3 required matrix + PHP 8.4 best-effort job + static-analysis job) |

**Important scope note on SEC-M4:** the audit chain adds tamper-*evidence*, not tamper-*immutability* — rows remain physically mutable at the database layer (this codebase has no append-only/WORM storage layer). What SEC-M4 guarantees is that any row edited or deleted out from under the hash chain becomes *detectable* by `AuditLogger::verifyIntegrity()` / `wp ai-os audit verify`, not that such an edit is prevented. Do not represent this as "immutable audit logs" in any customer-facing material.

**Important scope note on CI (REL-001/REL-002):** `composer install`, `phpstan`, and `phpcs` could not be executed in the authoring sandbox (no `composer` binary, no `vendor/`, no network access). The configs were validated structurally only (JSON/XML/YAML well-formedness, no tabs). The native WP-like suite and acceptance suite were re-verified green on PHP 8.2/8.3 after every change. **The first real GitHub Actions run of the `static-analysis` job is the true baseline** — treat any findings it reports as new work, not as a regression, since this codebase has never been statically analyzed before.

Full narrative: `docs/audits/SPRINT-0.3-SECURITY-CI-REPORT.md`.

## Confirmed bugs

| ID | Severity | Subsystem | File(s) | Line(s) | Problem | Impact | Root cause | Evidence | Recommended fix | Tests required | Dependency | Safe for Sprint 0.1? | **Status** |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| BUG-006 | Medium | Test infrastructure | `tests/shim/wp-functions.php` | `tableFor()` ~L1044-1056 and every independent table-name regex in `wpdb::query()`/`insert()`/`update()` (~L1111, ~L1151, ~L1177, ~L1268, ~L1273, ~L1303, ~L1428, ~L1432) | Every table-name-extraction regex in the fake `wpdb` is hardcoded `wp_([a-z_]+)` (letters/underscore only, no digit). Real WordPress's own multisite prefix for any site but the first is `wp_{blog_id}_...` — a leading digit `tableFor()`'s pattern cannot match. Separately, the shim's row storage (`$wpdb->tables[...]`) is keyed by the captured table SHORT name only, with no site component at all. | An `INSERT`/`UPDATE`/`SELECT` issued while `switch_to_blog()`'d to a non-primary site silently fails to parse against every existing repository — `wpdb::insert()` genuinely returns `false` (this was invisible until Sprint 0.3A Phase 2's exit-gate pass added fail-closed handling for a discarded `INSERT` return value, which then surfaced it as a thrown exception). Independently, even if the regex were fixed to match, the short-name-only storage key would put every site's rows in ONE shared bucket, actively breaking isolation rather than modeling it. Every existing "multisite isolation" test in this codebase (`ChangeSetRepositoryTest::test_change_set_rows_are_isolated_per_site`, `AuditIntegrityTest::test_multisite_chain_isolation_via_site_id`, `CapabilityManagementTest::test_capability_grants_are_isolated_per_site`, etc.) only ever WRITES on the default site and switches purely for a read-based absence check afterward — the only pattern the shim can support — so none of them have ever proven genuine per-site WRITE isolation; they pass today for the same "regex doesn't match, getRow() returns nothing" reason described above, not because of real isolated storage. | The shim was built incrementally, one regex per SQL shape actually exercised by single-site tests, without ever revisiting the multisite prefix format once multisite support was added in BUG-005/Sprint 0.1. Likely the same underlying gap this register's own T-018 forward-reference (in TEST-002 below) was anticipating without a concrete repro. | Confirmed directly this pass: `tests/Integration/JournalMultisiteIsolationTest.php`'s first draft attempted a real `OperationJournalRepository::create()`/`ChangeSetRepository::create()` call immediately after `switch_to_blog(2)` and hit `MutationException: Failed to persist the operation/changeset row` — traced to `preg_match('/insert\s+into\s+`?wp_([a-z_]+)`?.../, $sql, $m)` failing to match `INSERT INTO wp_2_ai_os_operation_journal (...)`. | Two-part fix, only safe to do together: (1) widen every `wp_([a-z_]+)` regex to `wp_(?:\d+_)?([a-z_]+)` so a non-primary site's table name parses at all, AND (2) change `$wpdb->tables`'s storage key from the short name alone to the full matched table name (or an equivalent site-aware composite key) so sites genuinely stop sharing one bucket. Part 2 requires updating every existing test that reaches into `$wpdb->tables['short_name']` directly (at least `ChangeSetRepositoryTest`, `OperationJournalRepositoryTest` — tamper/integrity tests use this pattern) to the new keying scheme. | A dedicated multisite-write regression: create on site 1 and site 2 with the SAME id/key, assert each site reads back only its own value, assert neither table's row count reflects the other site's write. | None beyond careful auditing of every existing `$wpdb->tables[...]` direct-access call site before changing the key scheme. | N/A — found in Sprint 0.3A, not Sprint 0.1 | **OPEN.** Explicitly not fixed this pass (Sprint 0.3A Phase 2 exit-gate closure, 2026-09-07) — judged too large/risky a shared-infrastructure change to make safely alongside the mutation-engine work in flight; `docs/ARCHITECTURE.md` §13 and `tests/Integration/JournalMultisiteIsolationTest.php`'s own docblock both document the finding. Real per-site WRITE isolation for every `ai_os_*` table remains provable only via the real WordPress/MySQL CI job (`test-real-wp-mysql` in `.github/workflows/ci.yml`, itself CI-CONFIGURED-NOT-RUN), not this shim, until BUG-006 is resolved. |
| BUG-001 | Critical | Filesystem security | `src/Security/PathGuard.php` | `isInsideRoot()` ~L124-129, `toAbsolute()` ~L224-249 | `isInsideRoot()` calls `toAbsolute()` on a path that is already absolute; `toAbsolute()` unconditionally rejects any path beginning with a Windows drive letter (`/^[a-z]:/i`). On Windows, `ABSPATH`-derived roots are drive-letter paths, so the guard's own internal re-derivation rejects itself. | Every `PathGuard::resolveRead()` call fails with `E_INVALID` on any Windows-hosted WordPress install. `FileReader::forTheme()`/`forRoot()`, and therefore `theme.read_file` and any future file-inspection tool, are completely unusable. | `isInsideRoot()` was written to accept "a path, relative or absolute" and always normalizes via `toAbsolute()`, but `toAbsolute()`'s drive-letter guard was written assuming it only ever sees *raw, untrusted, external* input — not the guard's own already-resolved absolute paths. | 6 direct failures in `tests/Unit/PathGuardTest.php` (`test_wp_config_is_protected`, `test_env_is_protected`, `test_plugin_own_directory_is_protected`, `test_disallowed_extension_is_rejected`, `test_missing_file_is_not_found`, `test_valid_read_resolves_to_real_path`) + 1 downstream failure (`tests/Integration/ToolExecutorTest.php::test_theme_read_file_works_for_real_files`), reproduced identically on PHP 8.2 and 8.3 with a full extension set (i.e., not an extension-availability artifact). | Make `isInsideRoot()` accept an already-absolute path without re-running the raw-input drive-letter/UNC rejection, or split `toAbsolute()` into a "normalize known-internal absolute path" step and a separate "validate untrusted raw input" step, and use the former internally. | Existing `PathGuardTest` suite must go green with no changes to the tests themselves; add an explicit Windows-drive-letter regression test. | None | Yes | **RESOLVED** — commit `a16f359`. `toAbsolute()` now classifies POSIX/drive-letter/UNC input as "absolute, let the confinement check decide" instead of rejecting drive letters outright; case-insensitive comparison added for Windows. All 20 `PathGuardTest` cases (11 original + 9 new) and the downstream `ToolExecutorTest` case pass. |
| BUG-002 | Critical | REST API | `src/Rest/Controllers/ToolsController.php` | `register()`, `tool` arg definition (~L48-53) | `POST /tools/execute`'s `tool` request argument uses `'sanitize_callback' => 'sanitize_key'`. WordPress's `sanitize_key()` strips every character outside `[a-z0-9_-]`, deleting the `.` that separates a tool's category from its action. | Every real tool name (all of which are dot-namespaced, e.g. `content.get_post`, enforced by `Tool::make()`'s own validation regex) is mangled before reaching `ToolRegistry::get()`, so it never matches. The endpoint always returns `tool.unknown` for every legitimate call. 100% of REST-direct tool execution is broken; the MCP `tools/call` surface is unaffected (it does not sanitize the name through this callback). | `sanitize_key()` was chosen as a generic "safe string" sanitizer without checking that it is incompatible with the dot-notation naming convention the tool/ability layer itself mandates. | Confirmed by static tracing: `Tool::make()` (`src/Tools/Tool.php` ~L91) requires `/^[a-z][a-z0-9_]*(\.[a-z0-9_]+)+$/i` (at least one dot); `sanitize_key()`'s documented behavior (WordPress core) strips non-`[a-z0-9_-]` characters including `.`. No existing test exercises this path (the WP shim does not implement the REST `args`/`sanitize_callback` pipeline), so it evades the current suite entirely — a real, currently-undetected regression. | Change the sanitize callback to something dot-safe, e.g. a custom callback restricted to `[a-z0-9_.]` (mirroring the tool-name regex), or drop the callback and rely on the executor's own `ToolRegistry::get()` returning `tool.unknown` for anything invalid (already a safe fallback). | Add a REST-layer integration test (or extend the shim to simulate `register_rest_route` arg sanitization) asserting `tool=content.get_post` round-trips unchanged through registration. | None | Yes | **RESOLVED** — commit `9bd693a`. Added `Tool::NAME_PATTERN` as the shared canonical grammar; `ToolsController` now uses a dot-safe `sanitizeToolIdentifier()` (trim + lowercase only, never strips characters) and a `validateToolIdentifier()` that rejects malformed/control-character/injection-shaped input with a structured 400 before the executor ever sees it. All 31 catalog tools verified to survive sanitization unchanged (`ToolExecutorTest::test_all_registered_tools_survive_rest_identifier_sanitization`). Full REST-transport round-trip (TEST-010) remains a Sprint 0.5 gap — see below. |
| BUG-003 | High | Test infrastructure | `tests/bootstrap.php`, `composer.json` | bootstrap: full file (17 lines); composer.json: `autoload` block | `phpunit.xml.dist` declares `bootstrap="tests/bootstrap.php"`, which loads the WP shim and the plugin, but never requires `tests/TestCase.php`. `composer.json`'s PSR-4 autoload map covers only `AIOS\\ → src/`, not the `AIOS\Tests\` namespace every test class lives in. | Running real PHPUnit (`vendor/bin/phpunit -c phpunit.xml.dist`, or any IDE/CI integration that invokes PHPUnit directly rather than `composer test`) fails immediately with `Class "AIOS\Tests\TestCase" not found` before a single test executes. | The bundled custom runner (`tests/run.php`) explicitly `require_once`s `TestCase.php` itself, masking the gap for anyone who only ever runs `composer test`; `phpunit.xml.dist` was written to reuse the same bootstrap without replicating that require. | Reproduced directly: downloaded `phpunit-10.phar` to `%TEMP%`, ran `php -c <wp-like-ini> phpunit-10.phar -c phpunit.xml.dist --colors=never` against the untouched repo — fatal error, exit code 255, zero tests run. | Add `require_once __DIR__ . '/TestCase.php';` to `tests/bootstrap.php` (matches what `tests/run.php` already does), and/or add a `AIOS\\Tests\\: tests/` entry to `composer.json`'s `autoload-dev` PSR-4 map so Composer's own autoloader (once `vendor/` exists) resolves it too. | Add a CI job that runs `vendor/bin/phpunit` (not just `tests/run.php`) so this class of drift cannot silently return. | Composer install (to get `vendor/bin/phpunit`) — but the `bootstrap.php` fix itself needs no dependency. | Yes | **RESOLVED** — commit `61d614c`. Bootstrap fix landed as planned; additionally discovered and fixed a deeper issue (fixing the class-not-found fatal alone still left PHPUnit executing zero tests, since the native `*Test` classes don't extend `PHPUnit\Framework\TestCase`) via `tests/PHPUnit/NativeSuiteBridgeTest.php`. `vendor/bin/phpunit -c phpunit.xml.dist` now reports `OK (17 tests, 51 assertions)`, zero warnings/deprecations, on PHP 8.2 and 8.3. |
| BUG-004 | High | Bootstrap / environment | `ai-wordpress-os.php` (`ai_wp_os_environment_failed()`), `src/Support/Crypto.php`, `src/Support/Validator.php`, `src/Support/Sanitize.php`, `src/Security/ApiKeyManager.php`, all four `src/Database/Repositories/*.php` | environment guard ~L60-70 of `ai-wordpress-os.php` | `openssl_encrypt`/`openssl_decrypt` (`Crypto`) and `mb_strlen`/`mb_substr` (`Validator`, `Sanitize`, `ApiKeyManager`, repositories) are called unconditionally. Neither `openssl` nor `mbstring` is checked by the plugin's own environment guard, which only checks PHP version and WP version. | On any host missing these (uncommon but real on minimal/embedded PHP builds), the plugin activates successfully and then fatals with an uncaught `Error: Call to undefined function` the first time an affected code path runs — mid-request, not at activation, with no admin-facing explanation. | The environment guard function was written once, early, for the two checks the spec called out explicitly (PHP/WP version), and was never revisited when `Crypto`/`Validator`/`Sanitize` were added as hard dependents of specific extensions. | Directly reproduced: `php -n tests/run.php` (bare PHP, no extensions) → 54 failures, every one an uncaught `Error: Call to undefined function AIOS\...\{openssl_encrypt,mb_strlen,mb_substr}()`; all 54 disappear once `mbstring`+`openssl` are loaded (confirmed on both PHP 8.2 and 8.3). | Extend `ai_wp_os_environment_failed()` (or a new sibling check run at activation) to verify `extension_loaded('mbstring')` and (`extension_loaded('openssl') OR extension_loaded('sodium')`), and fail activation with a clear admin notice rather than allowing a later uncaught fatal. | Add an activation-guard unit test asserting the plugin refuses to activate (with a structured message) when a required extension is simulated absent. | None | Yes | **RESOLVED** — commit `598ffea`. Chose graceful degradation over hard-blocking activation: `AIOS\Support\Strings` provides an mbstring-preferred, byte-safe fallback for every `mb_strlen`/`mb_substr` call site (all migrated); `Crypto::encrypt()`/`decrypt()` now guard `function_exists()` before calling `openssl_*`, throwing/returning cleanly instead of fataling when no backend exists. New `AIOS\Core\EnvironmentGuard` centralizes detection and reports degraded (non-fatal) capabilities via an admin notice, `wp ai-os status`, and the REST `/status` endpoint. Bare `php -n`: 183 tests pass, 4 fail (CryptoTest only — see note below); with any WP-realistic extension set: 187/187. |
| BUG-005 | High | WordPress lifecycle / multisite | `src/Core/Activator.php`, `uninstall.php` | `Activator::activate()` full file; `uninstall.php` L37-46 | `Activator::activate(bool $network_wide)` accepts but never reads `$network_wide`; it runs the migrator exactly once against the current site's `$wpdb->prefix`. `uninstall.php` drops tables using the bare `$wpdb->prefix` with no loop over `get_sites()`. | On network activation, only the initiating site gets its four `ai_os_*` tables; every other site in the network will hit "table doesn't exist" SQL errors the first time any AI OS feature runs there. On multisite uninstall with data-removal opted in, every site's tables except the one that ran the deletion are permanently orphaned. | Phase 1 was built and tested against a single-site mental model; the `$network_wide` parameter was added to match WordPress's activation-hook signature but never wired to actual per-site provisioning logic. | Confirmed by static reading of `Activator::activate()` (parameter unused) and `uninstall.php` (single `$wpdb->prefix`, no `switch_to_blog()`/`get_sites()` loop; the existing `is_multisite()` branch only covers sitemeta-level transient cleanup). No test suite in the repo boots a multisite shim, so this is untested by construction, not merely unverified. | Either (a) loop `get_sites()` + `switch_to_blog()`/`restore_current_blog()` in both `Activator::activate()` and `uninstall.php` to provision/clean every site, or (b) explicitly declare and enforce single-site-only support (reject network activation) until multisite is a planned feature. | Add a multisite-shim-based activation test (create N fake sites, assert N sets of tables exist) once a multisite test harness exists (see Test Gaps). | Requires a multisite-capable test shim (currently single-site only) — a prerequisite gap in its own right. | Partially — the "explicitly reject network-wide activation with a clear message" half of option (b) is Sprint-0.1-safe; full multisite provisioning is not (needs the shim work first). | **RESOLVED** (option (a), full provisioning — chosen over rejection) — commit `b85feb0`. Built the prerequisite multisite-capable shim (blog-scoped options/transients, `get_sites()`/`switch_to_blog()`/`restore_current_blog()`, `dbDelta()` table-name logging) this fix itself needed, then implemented the full `get_sites()`/`switch_to_blog()` loop in both `Activator::activate()` and `uninstall.php`, plus new-site provisioning via `wp_insert_site`. 11/11 `MultisiteLifecycleTest` cases pass. Real-WordPress multisite integration testing (vs. this shim) remains Sprint 0.7 scope (TEST-002 downgraded to partial below, not fully closed). |

## Likely bugs (strong evidence, not independently execution-confirmed)

| ID | Severity | Subsystem | File(s) | Problem | Why "likely" not "confirmed" | Recommended fix | Tests required |
|---|---|---|---|---|---|---|---|
| LIKELY-001 | Medium | Security / REST | `src/Mcp/Transports/RestTransport.php::isLocalDevelopment()` | `require_https` is bypassed whenever `HTTP_HOST` contains `localhost`, `127.0.0.1`, `.local`, or `.test`. On a misconfigured reverse proxy / permissive default vhost, a client-supplied `Host` header could trigger this exemption on a production endpoint. | Exploitability depends entirely on hosting/proxy configuration not present in this repository; could not be tested against a real HTTP stack in this audit. | Prefer a server-side, non-client-controlled signal (e.g., `WP_ENVIRONMENT_TYPE` alone, or an explicit admin-configured allowlist) instead of trusting `HTTP_HOST`. | Add a unit test asserting `isLocalDevelopment()` returns false for a spoofed `Host` header when `WP_ENVIRONMENT_TYPE` is not `local`. |
| LIKELY-002 | Medium | Security / concurrency | `src/Security/RateLimiter.php::allow()` | Non-atomic transient read-increment-write; concurrent requests within the same window can each pass the check before any persists its increment. | The code's own comment acknowledges the tradeoff explicitly ("acceptable here because limits are protective, not billing-precise") — flagged as "likely" because real-world bypass magnitude depends on the object-cache backend and request concurrency, not verified under load in this audit. | If the rate limiter must ever be a hard ceiling (e.g., cost control), replace with an atomic primitive (`wp_cache_incr()` against a real external object cache, or a DB-level atomic counter). | Add a concurrency test that fires N parallel `allow()` calls against a mocked non-atomic transient store and asserts the over-limit count matches the known race window (documents the risk quantitatively rather than eliminating it, unless the fix above is also applied). |
| LIKELY-003 | Low | UI / release | `assets/js/admin-dashboard.js` vs. `build/dashboard-src/src/app.jsx` | No build-reproducibility check exists; cannot independently confirm the shipped minified bundle is the exact product of the current source without running `npm install && npm run build` (blocked by this audit's no-install policy). | Presumed in sync (single-commit baseline, both files added together) but unverified by execution. | Add a CI step that rebuilds the bundle from source and diffs it against the committed artifact (fails the build on drift), or stop committing the built artifact and build it at release-packaging time instead. | CI job, not a unit test. |

## Security issues

See `docs/audits/ENTERPRISE-READINESS-AUDIT.md` §13 for the full classified list (C-1, C-2 = BUG-001/BUG-002 above; H-1..H-3 = BUG-003/BUG-004/BUG-005 above; M-1..M-5 and L-1..L-4 are additional findings not risen to "bug" status — summarized here for completeness):

| ID | Severity | One-line summary | **Status** |
|---|---|---|---|
| SEC-M1 | Medium | Rate limiter non-atomic under concurrency (= LIKELY-002). | **RESOLVED** — commit `b093e84`. Replaced the transient read-increment-write with an atomic database-backed counter (`RateLimitRepository`, `INSERT ... ON DUPLICATE KEY UPDATE`-style single statement); holds under concurrent `allow()` calls for the same principal. |
| SEC-M2 | Medium | HTTPS bypass trusts client `Host` header (= LIKELY-001). | **RESOLVED** — commit `5eee086`. `isLocalDevelopment()` no longer inspects `HTTP_HOST`/any client-supplied header; the local-development exemption now derives solely from `wp_get_environment_type()` (a server-side, non-client-controlled signal). |
| SEC-M3 | Medium | `/tools/execute` is the only mutating REST endpoint without a defense-in-depth nonce check (core cookie-auth nonce enforcement still applies first). | **RESOLVED** — commit `765f2e1`. `ToolsController::execute()` now calls `verifyNonce()`, matching `ApprovalsController`/`SettingsController`/`KeysController`. 6 `ToolsControllerNonceTest` cases cover cookie/application-password/API-key/unauthenticated paths. |
| SEC-M4 | Medium | Audit log has no tamper-evidence (no hash chain / append-only enforcement). | **RESOLVED** — commit `f3d7aa3`. Added `AIOS\Audit\AuditIntegrity`: each row is HMAC-SHA256-chained to its predecessor (key domain-separated from `Crypto`'s). This is tamper-*evidence*, not tamper-*immutability* — rows are still physically mutable at the DB layer; a tampered/deleted row becomes *detectable* via `verifyIntegrity()`, not prevented. 22 `AuditIntegrityTest` cases + `AuditChainTest`. |
| SEC-M5 | Medium | `Crypto`'s environment-derived key has no rotation strategy; rotating `AUTH_KEY`/`AUTH_SALT` silently breaks decryption of anything encrypted under the old key (latent — no current call site persists `Crypto`-encrypted data yet). | **RESOLVED** — commit `3f4e246`. Replaced the single-byte-version wire format with an explicit, parseable envelope (`aios{ver}:{backend}:{key_version}:{base64}`, non-secret metadata only). Adds current/previous key-version support, explicit failure codes (malformed/unsupported-version/unknown-key-version/auth-failed), safe legacy-ciphertext detection + decrypt (flagged, never silently accepted), and `reencrypt()` for migrating ciphertext to the current key version without the caller ever handling plaintext. 31 `CryptoTest` cases. Still latent in the sense that no call site persists `Crypto`-encrypted data yet — the rotation mechanism is now ready for whenever one does. |
| SEC-L1 | Low | `Container::has()`/`get()` inconsistency for uninstantiable classes. | OPEN — not in Sprint 0.3A scope. |
| SEC-L2 | Low | `ApiKeyRepository::revoke()` fetches up to 500 rows to find one by id. | OPEN — tracked as T-020, Sprint 0.4. |
| SEC-L3 | Low | Escalation blocklist has zero live test coverage (targets tools that don't exist yet). | OPEN — not in Sprint 0.3A scope. |
| SEC-L4 | Low | `PathGuard` allows reading `.php` files by design — confirmed intentional, flagged for re-confirmation before any Phase-2 write engine ships. | OPEN — re-confirm before Phase 2 mutation engine writes to `.php` files. |

SEC-M1..M5 were Sprint 0.3 hardening items, out of scope for Sprint 0.1 — Sprint 0.1's security regression pass confirmed none were made worse by the BUG-001..005 fixes. All five are now `RESOLVED` as of Sprint 0.3A (2026-09-07, see the resolution summary above). SEC-L1..L4 remain `OPEN`, out of Sprint 0.3A's scope.

## Architecture gaps (expected at this phase — not bugs)

| ID | Gap | Why it's out of scope for Sprint 0.1 |
|---|---|---|
| ARCH-001 | No Transaction Engine / ChangeSet model | Explicitly a Phase-2+ roadmap item; Phase 1 is read-only + safe/approval-gated writes only. |
| ARCH-002 | No Snapshot/Diff/Rollback engine | Same as above; depends on ARCH-001. |
| ARCH-003 | No background job / async execution system | All tool execution today is synchronous within the HTTP request; acceptable at current tool complexity, will not scale to long-running Phase-2 operations (large content migrations, staging syncs). |
| ARCH-004 | No idempotency-key / replay protection for MCP `tools/call` | A retried mutating call (e.g., after a client timeout) can double-execute. Documented as a Phase-2-adjacent hardening item, not blocking Phase 1's own correctness. |
| ARCH-005 | **RESOLVED** (role default: commit `c1fbf34`; admin-grantable layer: commit `ea452db`) — No RBAC/team support beyond raw WordPress capabilities | `ai_os_use`/`ai_os_approve` are granted to the `administrator` role at activation (unchanged from Sprint 0.1). Sprint 0.3A adds `AIOS\Security\CapabilityManager`: a conservative, whitelist-only (these two capabilities, nothing else) admin-controlled grant/revoke layer for individual non-administrator users, via real `WP_User::add_cap()`/`remove_cap()` (per-site, multisite-safe) — no self-escalation, unauthorized callers refused, every change audited. REST surface: `GET /capabilities/{id}`, `POST /capabilities/grant`, `POST /capabilities/revoke`. 21 `CapabilityManagementTest` cases, including "an explicitly granted non-admin approver passes the real approval gate; an equivalent ungranted user does not." This is still deliberately NOT a general RBAC system (no custom roles, no capability inheritance, no team/group model) — see the Phase 2+ roadmap for that. |
| ARCH-006 | No versioned API contract / deprecation strategy for the REST or MCP surfaces | Fine for a v1.0.0 baseline; becomes necessary the moment a breaking change is needed post-release. |
| ARCH-007 | No feature-flag system | All behavior is controlled by the single `Settings` mode/threshold model; adequate for Phase 1's scope. |

## Test gaps

| ID | Missing coverage | Notes |
|---|---|---|
| TEST-001 | Real WordPress REST transport (routing, `permission_callback` wiring under an actual `WP_REST_Server`, application-password auth) | **Still OPEN.** The shim never boots a real REST server; this is exactly the class of gap that let BUG-002 (`sanitize_key`) go undetected. Sprint 0.5 scope. |
| TEST-002 | Multisite (activation, uninstall, context, rate limiting, capability checks across `switch_to_blog()`) | **PARTIALLY RESOLVED** — a multisite-capable shim now exists (blog-scoped options/transients, `get_sites()`/`switch_to_blog()`/`restore_current_blog()`) and BUG-005's fix is covered by 11 `MultisiteLifecycleTest` cases (activation, uninstall, new-site provisioning, deactivation). Still open: real-WordPress multisite integration testing (this shim is a deliberately-scoped test double, not a full `$wpdb` SQL-prefix engine — see T-018) and multisite coverage of context caching / per-site rate limiting specifically. Sprint 0.7 scope for the remainder. |
| TEST-003 | Real MySQL/MariaDB `dbDelta()` execution | Still OPEN. Only a test double is exercised; real charset/collation/index-creation behavior on MySQL/MariaDB is unverified. |
| TEST-004 | Schema upgrade / migration-versioning path (applying migration N to a site already on migration N-1 with real pre-existing data) | Still OPEN. Only "fresh install, all migrations pending" is exercised. |
| TEST-005 | Uninstall behavior (`uninstall.php`) under both `remove_data_on_uninstall = true` and `= false` | **RESOLVED** — `MultisiteLifecycleTest` and `CapabilityLifecycleTest` both invoke the real `uninstall.php` in-process under both retention states (single-site and multisite). |
| TEST-006 | Concurrency / race conditions (parallel rate-limiter hits, parallel approval claims under real DB locking, parallel migration runs) | Still OPEN. `ApprovalRepository::claimPending()`'s correctness is inferred from code reading, not exercised under real concurrent load. Sprint 0.7 scope. |
| TEST-007 | WP-CLI command execution (`wp ai-os status\|migrate\|tools`) | Still OPEN. `WP_CLI_Command` is referenced, never instantiated by any test. |
| TEST-008 | Browser/admin-UI tests for the React console | Still OPEN. No JS test runner or browser automation exists in the repository. |
| TEST-009 | Security regression tests for the specific bugs in this register (BUG-001..005) | **RESOLVED** — every Sprint 0.1 fix shipped with regression tests per the recommendations above (see the resolution summary table). |
| TEST-010 | REST-layer sanitize/validate pipeline simulation | **PARTIALLY RESOLVED** — `ToolsController::sanitizeToolIdentifier()`/`validateToolIdentifier()` are now unit-tested directly as pure functions (16 cases in `ToolIdentifierTest`), closing the specific gap that let BUG-002 go undetected. The shim still does not implement `register_rest_route()`'s `args`/`sanitize_callback`/`validate_callback` processing end-to-end, so no controller's declared `args` schema is exercised through an actual REST request/response cycle. Sprint 0.5 scope for the remainder. |

## Environment / tooling gaps

| ID | Gap |
|---|---|
| ENV-001 | No system PHP or Composer installed on a representative fresh Windows dev machine (confirmed during this audit) — the plugin cannot assume any PHP tooling pre-exists; onboarding documentation should say so explicitly. |
| ENV-002 | PHP 8.4 could not be exercised on this host (VC++ runtime mismatch: plugin build vs17/14.44 vs. host's installed 14.22) — not a plugin defect, but confirms PHP 8.4 compatibility is currently *unverified*, not merely "should work because `composer.json` allows it." |
| ENV-003 | No `vendor/` directory and no `composer.lock` exist anywhere in version control or on disk — `composer install` has never been run against this baseline. |
| ENV-004 | No `node_modules/` and no JS lockfile exist for `build/dashboard-src` — the JS build has never been run against this baseline either. |

## Release gaps

| ID | Gap | Status |
|---|---|---|
| REL-001 | No CI configuration of any kind (`.github/workflows/` absent). | **RESOLVED** — commit `8efb2de`. `.github/workflows/ci.yml`: PHP 8.2/8.3 required matrix (lint, native suite, PHPUnit bridge, acceptance) + PHP 8.4 best-effort job + static-analysis job. Not yet executed against a real GitHub Actions runner as of this sprint (see the CI scope note above). |
| REL-002 | No static analysis configuration (`phpstan.neon*`, `phpcs.xml*` both absent). | **RESOLVED** — commit `8efb2de`. `phpstan.neon.dist` (level 5, WordPress-aware via `szepeviktor/phpstan-wordpress` + `php-stubs/wordpress-stubs`, scoped to `src/`) and `phpcs.xml.dist` (WordPress-Extra, one structural exclusion for this Composer/PSR-4 codebase's file naming). Neither has been run yet in this sandbox (no composer/network) — first CI run establishes the real baseline. |
| REL-003 | No dependency-audit tooling (`composer audit`/`npm audit` not wired into any script; no lockfiles to audit against in the first place). | OPEN — tracked as T-029/T-030, Sprint 0.6. Not in Sprint 0.3A scope. |
| REL-004 | No build-reproducibility check for the shipped JS bundle (see LIKELY-003). | OPEN — tracked as T-030, Sprint 0.6. Not in Sprint 0.3A scope. |

## Future features (explicitly out of scope for stabilization — tracked for Phase 2+)

Transaction Engine, ChangeSet model, Snapshot Engine, Diff Engine, guarded mutation system, verification engine, rollback system, background jobs, Gutenberg/Elementor/WooCommerce/ACF/MetaBox/Pods adapters, form integrations, SEO integrations, staging/deployment, visual/browser QA, multi-agent orchestration, enterprise team/RBAC. See `docs/roadmap/ENTERPRISE-ROADMAP.md` for full sequencing.
